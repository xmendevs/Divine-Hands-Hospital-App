# Stage 4: Authentication & Settings Pages

## Goal
Polish `LoginPage.tsx` and restructure the multi-section `SettingsPage.tsx` into structured card panels.

## Context & Rules
- Do not modify API payloads, URLs, or backend configuration keys.
- Preserve first-run admin credential display and backup execution flows.
- Use `@hims/ui` primitives (`Card`, `FormField`, `Button`, `DataTable`, `Badge`).

## Prompt to AI Agent
Upgrade `LoginPage.tsx` and `SettingsPage.tsx` in `apps/desktop/src/pages/` using design tokens and the `@hims/ui` kit.

1. **`LoginPage.tsx`:**
   - Card elevation using `theme.shadow.popover` and `radius.lg` on canvas `#f8fafc`.
   - Use `FormField`, `Input`, and `Button` with embedded loading spinner.
   - Clean toggle for "Connection settings" (server IP configuration).
   - Retain all auth state, TOTP MFA validation, and error messaging.
2. **`SettingsPage.tsx`:**
   - Use `PageHeader` at the top (`Title: System Settings & Administration`).
   - Segment the settings view into structured `Card` blocks:
     - **Card 1: Server Connection** (LAN Base URL + test connection action).
     - **Card 2: Hospital Server (This Machine)** (first sign-in credentials block with copy button, port configurations).
     - **Card 3: Backup & Cloud Storage** (Neon connection status badge, automated backup frequency, "Run backup now" / "Upload to cloud now" buttons).
     - **Card 4: Backup Activity & Logs** (`DataTable` for recent backup jobs with status badges).
     - **Card 5: User Account & Password** (change password form with validation).
   - Preserve every configuration key, API call payload, and status test flow.

## Verification Steps
- Run `pnpm typecheck && pnpm lint && pnpm test && pnpm build`.
- Exercise Login, MFA prompt, Connection settings toggle, and Settings card interactions in `pnpm dev`.