# Stage 3: App Shell, Sidebar & Navigation

## Goal
Overhaul `apps/desktop/src/App.tsx` and extract a structured Sidebar matching the reference layout (deep navy container, uppercase grouping categories, active pill selection).

## Context & Rules
- Retain all navigation routes, tabs, and active tab switching logic.
- Keep `mustChangePassword` routing and auth state transitions intact.
- Use tokens and `@hims/ui` primitives.

## Prompt to AI Agent
Refactor the desktop app shell in `apps/desktop/src/App.tsx` to match the target reference navigation structure using `@hims/ui`.

1. **Extract `Sidebar` Component (`apps/desktop/src/ui/Sidebar.tsx`):**
   - Dark background (`#0b132b` / `#0f172a`), fixed width (`240px`), full viewport height.
   - Category section headers formatted as uppercase, tracked small text (`0.65rem`, font-weight 700, color `#64748b`):
     - **`CLINICAL`**: Patients Directory (`users`), Orders & Clinical (`clipboard`), Lab & Pathology (`flask`).
     - **`PHARMACY & INVENTORY`**: Pharmacy Dispense (`pill`), Hospital Inventory & Assets (`box`).
     - **`FINANCE & BILLING`**: Billing & Cashier (`cash`).
     - **`STAFF & OPERATIONS`**: Staff (`users`), Roster (`calendar`), Shift Handover Log (`book`), Staff Communications (`chat`), Reports (`file-text`).
     - **`SYSTEM & ADMIN`**: Settings (`gear`), Sign out (`logout`).
   - **Nav Item Styling:**
     - Inactive: transparent background, color `#94a3b8`, 6px radius, hover effect (`rgba(255,255,255,0.05)`).
     - Active: rounded pill background `#1e293b`, text `#ffffff` or `#38bdf8`, font-weight 600.
     - Render icon aligned to the left of the label.
2. **Main Layout Container:**
   - Right-side main viewport: light background (`#f8fafc`), flex column, full height with independent scrolling for page content.
3. **Accessibility & State:**
   - Add `role="navigation"` to sidebar, `aria-current="page"` to the active item, and visible focus outlines.
   - Maintain exact active-tab routing and session persistence.
4. **Tests:** Update `App.test.tsx` to verify that all sidebar categories and nav labels render.

## Verification Steps
- Run `pnpm typecheck && pnpm lint && pnpm test && pnpm build`.
- Inspect sidebar layout in dev mode (`pnpm dev`) to ensure it scales correctly across window sizes.