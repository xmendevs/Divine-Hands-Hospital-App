openapi: 3.1.0
info:
  title: HIMS go-api
  version: 0.4.0
  description: Core service API for the Divine Hands HMS (identity, RBAC, sessions, audit).
servers:
  - url: http://localhost:8080
tags:
  - name: system
    description: Health, readiness, and metadata endpoints.
  - name: auth
    description: Authentication, sessions, and MFA.
  - name: patients
    description: Patient registration, master record, clinical, timeline, documents.
  - name: families
    description: Family profiles and membership.
  - name: orders
    description: Doctor orders, lifecycle, and medication administration.
  - name: clinical
    description: Notes, observations, tasks, reports, triage, and assignments.
paths:
  /health:
    get:
      operationId: health
      summary: Liveness probe
      tags: [system]
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Service is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /ready:
    get:
      operationId: readiness
      summary: Readiness probe
      tags: [system]
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Service is ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
        "503":
          description: Service is not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
  /api/v1/version:
    get:
      operationId: version
      summary: Service metadata
      tags: [system]
      parameters:
        - $ref: "#/components/parameters/RequestId"
      responses:
        "200":
          description: Service metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /api/v1/auth/login:
    post:
      operationId: login
      summary: Authenticate and create a session
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          description: Invalid credentials or MFA required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /api/v1/auth/me:
    get:
      operationId: me
      summary: Current user, roles, and permissions
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
  /api/v1/auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Session revoked
  /api/v1/auth/password-reset/request:
    post:
      operationId: requestPasswordReset
      summary: Request a password reset
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username]
              properties:
                username:
                  type: string
      responses:
        "202":
          description: Accepted (response is identical whether or not the account exists)
  /api/v1/auth/password-reset/confirm:
    post:
      operationId: confirmPasswordReset
      summary: Complete a password reset
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, newPassword]
              properties:
                token:
                  type: string
                newPassword:
                  type: string
      responses:
        "204":
          description: Password updated
        "400":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /api/v1/auth/mfa/setup:
    post:
      operationId: mfaSetup
      summary: Generate a TOTP secret for enrollment
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: TOTP secret and provisioning URL
          content:
            application/json:
              schema:
                type: object
                required: [secret, otpauthUrl]
                properties:
                  secret:
                    type: string
                  otpauthUrl:
                    type: string
  /api/v1/auth/mfa/confirm:
    post:
      operationId: mfaConfirm
      summary: Verify a TOTP code and enable MFA
      tags: [auth]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
      responses:
        "204":
          description: MFA enabled
  /api/v1/patients:
    post:
      operationId: registerPatient
      summary: Register a patient
      tags: [patients]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPatientRequest"
      responses:
        "201":
          description: Registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Patient"
        "409":
          description: Duplicate patient
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /api/v1/patients/search:
    get:
      operationId: searchPatients
      summary: Search patients by ID or permitted demographics
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Matching patients
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PatientSummary"
  /api/v1/patients/{id}:
    get:
      operationId: getPatient
      summary: Get a patient (clinical only with clinical.view)
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Patient
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Patient"
        "404":
          description: Not found
    patch:
      operationId: updatePatient
      summary: Update patient demographics
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatientRequest"
      responses:
        "204":
          description: Updated
  /api/v1/patients/{id}/amend:
    post:
      operationId: amendPatient
      summary: Correct a patient-level field (records an amendment)
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [fieldName, newValue, reason]
              properties:
                fieldName:
                  type: string
                newValue:
                  type: string
                reason:
                  type: string
      responses:
        "204":
          description: Amended
  /api/v1/patients/{id}/clinical:
    get:
      operationId: listClinical
      summary: List a patient's clinical entries
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Clinical entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClinicalEntry"
    post:
      operationId: addClinical
      summary: Add a clinical entry
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [section, summary]
              properties:
                section:
                  type: string
                  enum:
                    [
                      allergy,
                      medical_history,
                      surgical_history,
                      chronic_condition,
                      medication,
                      family_history,
                      social_history,
                    ]
                summary:
                  type: string
                details:
                  type: object
                  additionalProperties: true
      responses:
        "201":
          description: Created
  /api/v1/patients/{id}/clinical/{entryId}:
    patch:
      operationId: amendClinical
      summary: Correct a clinical entry (records an amendment)
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: entryId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                summary:
                  type: string
                details:
                  type: object
                  additionalProperties: true
                reason:
                  type: string
      responses:
        "204":
          description: Amended
  /api/v1/patients/{id}/timeline:
    get:
      operationId: listTimeline
      summary: List a patient's timeline
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Timeline events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TimelineEvent"
  /api/v1/patients/{id}/documents:
    get:
      operationId: listDocuments
      summary: List a patient's documents
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Documents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Document"
    post:
      operationId: addDocument
      summary: Record document metadata
      tags: [patients]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                documentType:
                  type: string
                title:
                  type: string
                fileName:
                  type: string
                contentType:
                  type: string
                fileSize:
                  type: integer
                  format: int64
      responses:
        "201":
          description: Created
  /api/v1/families:
    post:
      operationId: createFamily
      summary: Create a family profile
      tags: [families]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [familyName]
              properties:
                familyName:
                  type: string
                headPatientId:
                  type: string
                  format: uuid
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Family"
  /api/v1/families/{id}:
    get:
      operationId: getFamily
      summary: Get a family profile with members
      tags: [families]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Family
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Family"
        "404":
          description: Not found
  /api/v1/patients/{id}/orders:
    post:
      operationId: createOrder
      summary: Create a doctor order
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderType]
              properties:
                orderType:
                  type: string
                  enum: [prescription, lab_request, nursing_order, referral]
                departmentId: { type: string, format: uuid }
                details:
                  type: object
                  additionalProperties: true
                noteId: { type: string, format: uuid }
                submit: { type: boolean }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Order" }
    get:
      operationId: listPatientOrders
      summary: List a patient's orders
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Order" }
  /api/v1/orders/actionable:
    get:
      operationId: listActionableOrders
      summary: List orders awaiting/under nursing action
      tags: [orders]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Order" }
  /api/v1/orders/{id}/submit:
    post:
      operationId: submitOrder
      summary: Submit a draft order
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204": { description: Submitted }
  /api/v1/orders/{id}/cancel:
    post:
      operationId: cancelOrder
      summary: Cancel a draft or submitted order
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string }
      responses:
        "204": { description: Cancelled }
  /api/v1/orders/{id}/status:
    post:
      operationId: transitionOrder
      summary: Advance an order to accepted/in_progress/completed
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [accepted, in_progress, completed] }
      responses:
        "204": { description: Transitioned }
  /api/v1/patients/{id}/administrations:
    post:
      operationId: addAdministration
      summary: Record a medication administration
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderId]
              properties:
                orderId: { type: string, format: uuid }
                medication: { type: string }
                dose: { type: string }
                route: { type: string }
                notes: { type: string }
      responses:
        "201": { description: Recorded }
    get:
      operationId: listAdministrations
      summary: List medication administration records
      tags: [orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Records
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Administration" }
  /api/v1/patients/{id}/notes:
    post:
      operationId: createNote
      summary: Create a clinical note
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [note]
              properties:
                noteType: { type: string, enum: [consultation, nursing, progress] }
                departmentId: { type: string, format: uuid }
                note: { type: string }
                diagnosis: { type: string }
                treatmentPlan: { type: string }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Note" }
    get:
      operationId: listNotes
      summary: List current notes
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Notes
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Note" }
  /api/v1/patients/{id}/notes/{groupId}:
    get:
      operationId: listNoteVersions
      summary: List all versions of a note
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: groupId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Versions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Note" }
  /api/v1/patients/{id}/notes/{groupId}/versions:
    post:
      operationId: addNoteVersion
      summary: Append a new immutable note version
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: groupId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [note]
              properties:
                departmentId: { type: string, format: uuid }
                note: { type: string }
                diagnosis: { type: string }
                treatmentPlan: { type: string }
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Note" }
  /api/v1/patients/{id}/observations:
    post:
      operationId: addObservation
      summary: Record vitals/observations
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                category: { type: string, enum: [vitals, observation] }
                measurements:
                  type: object
                  additionalProperties: true
                notes: { type: string }
      responses:
        "201": { description: Recorded }
    get:
      operationId: listObservations
      summary: List observations
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: category
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Observations
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Observation" }
  /api/v1/tasks:
    post:
      operationId: createTask
      summary: Create a department task
      tags: [clinical]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                patientId: { type: string, format: uuid }
                departmentId: { type: string, format: uuid }
                title: { type: string }
                description: { type: string }
                assignedToUserId: { type: string, format: uuid }
      responses:
        "201": { description: Created }
    get:
      operationId: listTasks
      summary: List tasks
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Tasks
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Task" }
  /api/v1/tasks/{id}/complete:
    post:
      operationId: completeTask
      summary: Complete a task
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204": { description: Completed }
  /api/v1/patients/{id}/admissions:
    post:
      operationId: admitPatient
      summary: Admit a patient
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ward]
              properties:
                ward: { type: string }
                room: { type: string }
                bed: { type: string }
                attendingDoctorId: { type: string, format: uuid }
                admissionReason: { type: string }
      responses:
        "201":
          description: Admitted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Admission" }
    get:
      operationId: listAdmissions
      summary: List a patient's admissions
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Admissions
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Admission" }
  /api/v1/patients/{id}/admissions/{admissionId}/discharge:
    post:
      operationId: dischargePatient
      summary: Discharge an active admission
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: admissionId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [dischargeSummary]
              properties:
                dischargeSummary: { type: string }
                followUpInstructions: { type: string }
      responses:
        "200":
          description: Discharged
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Admission" }
  /api/v1/patients/{id}/reports:
    post:
      operationId: createReport
      summary: Create a clinical report
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, content]
              properties:
                reportType: { type: string }
                title: { type: string }
                content: { type: string }
                departmentId: { type: string, format: uuid }
      responses:
        "201": { description: Created }
    get:
      operationId: listReports
      summary: List a patient's reports
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Reports
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Report" }
  /api/v1/clinical/triage:
    post:
      operationId: triage
      summary: Register and triage an emergency patient
      tags: [clinical]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chiefComplaint, triageLevel]
              properties:
                firstName: { type: string }
                lastName: { type: string }
                gender: { type: string }
                chiefComplaint: { type: string }
                triageLevel: { type: string }
                measurements:
                  type: object
                  additionalProperties: true
      responses:
        "201":
          description: Triaged
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TriageResponse" }
  /api/v1/clinical/queue:
    get:
      operationId: myQueue
      summary: The current clinician's assigned patients
      tags: [clinical]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Queue
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/QueueItem" }
  /api/v1/patients/{id}/assignments:
    post:
      operationId: assignPatient
      summary: Assign a patient to a clinician
      tags: [clinical]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assigneeUserId]
              properties:
                assigneeUserId: { type: string, format: uuid }
                departmentId: { type: string, format: uuid }
      responses:
        "201": { description: Assigned }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  parameters:
    RequestId:
      name: X-Request-ID
      in: header
      required: false
      description: Correlation ID echoed back on the response and in logs.
      schema:
        type: string
    Id:
      name: id
      in: path
      required: true
      description: Resource internal UUID.
      schema:
        type: string
        format: uuid
  schemas:
    HealthResponse:
      type: object
      required: [status, service, time]
      properties:
        status:
          type: string
          const: ok
        service:
          type: string
        time:
          type: string
          format: date-time
    CheckResult:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ok, error]
        error:
          type: string
    ReadyResponse:
      type: object
      required: [status, checks]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        checks:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/CheckResult"
    VersionResponse:
      type: object
      required: [service, version, timezone, utcNow]
      properties:
        service:
          type: string
        version:
          type: string
        timezone:
          type: string
        utcNow:
          type: string
          format: date-time
    User:
      type: object
      required: [id, username, email, status]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
        status:
          type: string
          enum: [pending, active, suspended]
    Role:
      type: object
      required: [id, code, name]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
        totpCode:
          type: string
        deviceName:
          type: string
    LoginResponse:
      type: object
      required: [token, expiresAt, mustChangePassword, user]
      properties:
        token:
          type: string
        expiresAt:
          type: string
          format: date-time
        mustChangePassword:
          type: boolean
        user:
          $ref: "#/components/schemas/User"
    MeResponse:
      type: object
      required: [id, username, email, status, mustChangePassword, mfaEnabled, roles, permissions]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
        status:
          type: string
        mustChangePassword:
          type: boolean
        mfaEnabled:
          type: boolean
        roles:
          type: array
          items:
            $ref: "#/components/schemas/Role"
        permissions:
          type: array
          items:
            type: string
    RegisterPatientRequest:
      type: object
      required: [firstName, lastName]
      properties:
        registrationType:
          type: string
          enum: [normal, antenatal, emergency]
        familyId:
          type: string
          format: uuid
        firstName:
          type: string
        lastName:
          type: string
        middleName:
          type: string
        gender:
          type: string
        dateOfBirth:
          type: string
          format: date
        bloodGroup:
          type: string
        genotype:
          type: string
        maritalStatus:
          type: string
        occupation:
          type: string
        phone:
          type: string
        alternatePhone:
          type: string
        email:
          type: string
        addressLine1:
          type: string
        addressLine2:
          type: string
        city:
          type: string
        state:
          type: string
        postalCode:
          type: string
        country:
          type: string
        identificationType:
          type: string
        identificationNumber:
          type: string
        nextOfKinName:
          type: string
        nextOfKinRelationship:
          type: string
        nextOfKinPhone:
          type: string
        consentGiven:
          type: boolean
        privacyNotes:
          type: string
        force:
          type: boolean
          description: Bypass the soft duplicate check (the database still enforces uniqueness).
    UpdatePatientRequest:
      type: object
      properties:
        firstName: { type: string }
        lastName: { type: string }
        middleName: { type: string }
        gender: { type: string }
        dateOfBirth: { type: string, format: date }
        bloodGroup: { type: string }
        genotype: { type: string }
        maritalStatus: { type: string }
        occupation: { type: string }
        phone: { type: string }
        alternatePhone: { type: string }
        email: { type: string }
        addressLine1: { type: string }
        addressLine2: { type: string }
        city: { type: string }
        state: { type: string }
        postalCode: { type: string }
        country: { type: string }
        identificationType: { type: string }
        identificationNumber: { type: string }
        nextOfKinName: { type: string }
        nextOfKinRelationship: { type: string }
        nextOfKinPhone: { type: string }
        consentGiven: { type: boolean }
        privacyNotes: { type: string }
    Patient:
      type: object
      required: [id, patientNo, registrationType, firstName, lastName, status]
      properties:
        id: { type: string, format: uuid }
        patientNo: { type: string }
        registrationType: { type: string, enum: [normal, antenatal, emergency] }
        familyId: { type: string, format: uuid, nullable: true }
        firstName: { type: string }
        lastName: { type: string }
        middleName: { type: string }
        gender: { type: string }
        dateOfBirth: { type: string, format: date, nullable: true }
        bloodGroup: { type: string }
        genotype: { type: string }
        maritalStatus: { type: string }
        occupation: { type: string }
        phone: { type: string }
        alternatePhone: { type: string }
        email: { type: string }
        addressLine1: { type: string }
        addressLine2: { type: string }
        city: { type: string }
        state: { type: string }
        postalCode: { type: string }
        country: { type: string }
        identificationType: { type: string }
        identificationNumber: { type: string }
        nextOfKinName: { type: string }
        nextOfKinRelationship: { type: string }
        nextOfKinPhone: { type: string }
        consentGiven: { type: boolean }
        consentDate: { type: string, format: date-time, nullable: true }
        privacyNotes: { type: string }
        status: { type: string, enum: [active, inactive, deceased, merged] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        clinical:
          type: array
          items:
            $ref: "#/components/schemas/ClinicalEntry"
    PatientSummary:
      type: object
      required: [id, patientNo, registrationType, firstName, lastName]
      properties:
        id: { type: string, format: uuid }
        patientNo: { type: string }
        registrationType: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        gender: { type: string }
        dateOfBirth: { type: string }
        phone: { type: string }
    ClinicalEntry:
      type: object
      required: [id, section, summary]
      properties:
        id: { type: string, format: uuid }
        section: { type: string }
        summary: { type: string }
        details:
          type: object
          additionalProperties: true
        recordedBy: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    TimelineEvent:
      type: object
      required: [id, eventType, summary, occurredAt]
      properties:
        id: { type: string, format: uuid }
        eventType: { type: string }
        summary: { type: string }
        data:
          type: object
          additionalProperties: true
        actorUserId: { type: string, format: uuid, nullable: true }
        occurredAt: { type: string, format: date-time }
    Document:
      type: object
      required: [id, title]
      properties:
        id: { type: string, format: uuid }
        documentType: { type: string }
        title: { type: string }
        fileName: { type: string }
        contentType: { type: string }
        fileSize: { type: integer, format: int64 }
        uploadedBy: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    Family:
      type: object
      required: [id, familyNo, familyName]
      properties:
        id: { type: string, format: uuid }
        familyNo: { type: string }
        familyName: { type: string }
        headPatientId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        members:
          type: array
          items:
            $ref: "#/components/schemas/PatientSummary"
    Order:
      type: object
      required: [id, orderNo, patientId, orderType, status, orderedBy, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        orderNo: { type: string }
        patientId: { type: string, format: uuid }
        orderType: { type: string, enum: [prescription, lab_request, nursing_order, referral] }
        status:
          { type: string, enum: [draft, submitted, accepted, in_progress, completed, cancelled] }
        departmentId: { type: string, format: uuid, nullable: true }
        orderedBy: { type: string, format: uuid }
        details:
          type: object
          additionalProperties: true
        clinicalNoteId: { type: string, format: uuid, nullable: true }
        actedBy: { type: string, format: uuid, nullable: true }
        cancelledBy: { type: string, format: uuid, nullable: true }
        cancelReason: { type: string }
        createdAt: { type: string, format: date-time }
        submittedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        updatedAt: { type: string, format: date-time }
    Note:
      type: object
      required:
        [id, groupId, patientId, noteType, authorUserId, authorRole, note, version, createdAt]
      properties:
        id: { type: string, format: uuid }
        groupId: { type: string, format: uuid }
        patientId: { type: string, format: uuid }
        noteType: { type: string }
        departmentId: { type: string, format: uuid, nullable: true }
        authorUserId: { type: string, format: uuid }
        authorRole: { type: string }
        note: { type: string }
        diagnosis: { type: string }
        treatmentPlan: { type: string }
        version: { type: integer }
        createdAt: { type: string, format: date-time }
    Administration:
      type: object
      required: [id, orderId, medication, administeredBy, administeredAt]
      properties:
        id: { type: string, format: uuid }
        orderId: { type: string, format: uuid }
        medication: { type: string }
        dose: { type: string }
        route: { type: string }
        administeredBy: { type: string, format: uuid }
        administeredAt: { type: string, format: date-time }
        notes: { type: string }
    Observation:
      type: object
      required: [id, category, recordedBy, recordedAt]
      properties:
        id: { type: string, format: uuid }
        category: { type: string }
        measurements:
          type: object
          additionalProperties: true
        notes: { type: string }
        recordedBy: { type: string, format: uuid }
        recordedAt: { type: string, format: date-time }
    Task:
      type: object
      required: [id, title, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        patientId: { type: string, format: uuid, nullable: true }
        departmentId: { type: string, format: uuid, nullable: true }
        orderId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        description: { type: string }
        status: { type: string }
        assignedTo: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }
    Admission:
      type: object
      required: [id, patientId, ward, status, admittedAt]
      properties:
        id: { type: string, format: uuid }
        patientId: { type: string, format: uuid }
        ward: { type: string }
        room: { type: string }
        bed: { type: string }
        admittedAt: { type: string, format: date-time }
        attendingDoctorId: { type: string, format: uuid, nullable: true }
        admissionReason: { type: string }
        status: { type: string, enum: [admitted, discharged] }
        dischargedAt: { type: string, format: date-time, nullable: true }
        dischargeSummary: { type: string }
        followUpInstructions: { type: string }
    Report:
      type: object
      required: [id, title, content, authorId, createdAt]
      properties:
        id: { type: string, format: uuid }
        reportType: { type: string }
        title: { type: string }
        content: { type: string }
        authorId: { type: string, format: uuid }
        departmentId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    QueueItem:
      type: object
      required: [assignmentId, patientId, patientNo, firstName, lastName, assignedAt]
      properties:
        assignmentId: { type: string, format: uuid }
        patientId: { type: string, format: uuid }
        patientNo: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        gender: { type: string }
        dateOfBirth: { type: string }
        phone: { type: string }
        assignedAt: { type: string, format: date-time }
    TriageResponse:
      type: object
      required: [patient, triage]
      properties:
        patient: { $ref: "#/components/schemas/Patient" }
        triage:
          type: object
          required: [id, patientId, triageLevel, chiefComplaint, createdAt]
          properties:
            id: { type: string, format: uuid }
            patientId: { type: string, format: uuid }
            triageLevel: { type: string }
            chiefComplaint: { type: string }
            measurements:
              type: object
              additionalProperties: true
            createdAt: { type: string, format: date-time }
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, requestId]
          properties:
            code:
              type: string
            message:
              type: string
            requestId:
              type: string
