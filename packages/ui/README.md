# ui

Shared React components for the Divine Hands HMS desktop client.

Consumed as `@hims/ui` from the desktop app. Source is exported directly (no
build step); `tsc --noEmit` typechecks and `vitest` runs component tests.
