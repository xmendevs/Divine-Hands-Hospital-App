# Server edition: same desktop app, plus the bundled server payload
# (server-payload -> resources/server) so the installed app can start
# PostgreSQL + the Go API itself - no terminal, no admin required.
# Built by: pnpm dist:server
appId: com.divinehands.hospital
productName: Divine Hands Hospital Server
executableName: divine-hands-hospital-server
directories:
  buildResources: build
  output: release-server
files:
  - dist/**/*
  - main.cjs
  - preload.js
  - package.json
extraResources:
  - from: server-payload
    to: server
win:
  icon: build/icon.png
  target:
    - target: nsis
      arch:
        - x64
linux:
  icon: build/icon.png
  category: Medical
  maintainer: Divine Hands Hospital <chuks4brown@gmail.com>
  artifactName: divine-hands-hospital-server-${version}.${arch}.${ext}
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Divine Hands Hospital Server
