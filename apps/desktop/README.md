# Desktop client

Tauri 2 + React + TypeScript desktop client for the Divine Hands HMS.

- Dev: `pnpm tauri dev`
- Build: `pnpm build` (frontend) / `pnpm tauri build` (bundled app)

See the repository root `README.md` for the full stack and how to run backing
services (PostgreSQL, Redis, MinIO) during development.
